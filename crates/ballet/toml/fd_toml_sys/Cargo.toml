[package]
name = "fd_toml_sys"
version.workspace = true
edition.workspace = true
build = "build.rs"
links = "fdtoml"

[dependencies]

[build-dependencies]
bindgen = { version = "0.72.1" }
cc = "1.0"

[dev-dependencies]