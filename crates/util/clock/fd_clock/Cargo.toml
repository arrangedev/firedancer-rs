[package]
name = "fd_clock"
version.workspace = true
edition.workspace = true

[dependencies]
fd_clock_sys = { path = "../fd_clock_sys" }
libc = "0.2"

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }

[[bench]]
name = "benchmarks"
harness = false

[[example]]
name = "usage"
