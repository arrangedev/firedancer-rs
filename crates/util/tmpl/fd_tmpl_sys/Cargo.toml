[package]
name = "fd_tmpl_sys"
version.workspace = true
edition.workspace = true
build = "build.rs"
links = "fdtmpl"

[dependencies]

[build-dependencies]
bindgen = "0.72.1"
cc = "1.0"
firedancer-rs-common = { path = "../../../common" }

[dev-dependencies]